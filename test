from openai import OpenAI
import json

client = OpenAI()

completion = client.chat.completions.create(
  model="gpt-3.5-turbo",
  messages=[
    {"role": "system", "content": "Eres un proveedor de software para empresas del sector energetico. Has comparado las variables calculadas por tu software 'Planning Space', con los valores brindados por un auditor y realizaste textos que explican las diferencias entre los valores del auditor y 'Planning Space'. Se te brindara: 'nombre del campo', 'tipo de campo', 'variable', 'mensaje'. Tu mision es devolver unicamente el texto de mensaje (sin comillas o texto adicional), con ortografía y sintaxis corregida a la perfección, teniendo en cuenta el contexto del mensaje."},
    {"role": "user", "content": "'nombre del campo'='Gibraltar'\n'tipo de campo'='PDP'\n'variable'='Variación OpEX Variable'\n'mensaje'='Quorum realizó la validación de calculo de Opex Variable utilizando VT del flujo de caja y los mismos valores de opex variable para el calculo del PXQ, pero no se logró igualar el calculo del auditor. Por el contario, esta misma validación confirmó el calculo realizado por Planingspace \nQuorum realizó la validación de calculo de Opex Variable utilizando VT del flujo de caja y los mismos valores de opex variable para el calculo del PXQ, pero no se logró igualar el calculo del auditor. Por el contario, esta misma validación confirmó el calculo realizado por Planingspace \n'"}
  ]
)

print(completion.choices[0].message.content)